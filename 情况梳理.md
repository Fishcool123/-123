# Cursor 终端中文粘贴乱码问题 — 完整排查记录

> 创建时间：2026-06-23
> 涉及模型：DeepSeek → Qwen → DeepSeek（切换）

---

## 一、问题描述

在 Cursor IDE 的终端里运行 `claude`（Claude Code CLI），**粘贴中文会变成乱码**（UTF-8 字节被当成 Latin-1/Windows-1252 单字节显示）。

乱码示例：`å¤§å­¦ç©çèµæ`（实际是"大学物理资料"）

**键盘打字中文完全正常**，只有粘贴会乱码。

编辑器（右侧代码编辑区）粘贴中文正常，问题仅限终端。

---

## 二、环境信息

| 项目 | 值 |
|------|-----|
| OS | Windows 11 Home China 10.0.26200 |
| Cursor 版本 | 3.8.22（6月22日更新，前一天还是旧版） |
| Claude Code 扩展 | 2.1.186 |
| 终端 Shell | Git Bash 5.3.9 (x86_64-pc-cygwin) / MSYS2 3.6.7 |
| PowerShell 7 | 已安装（winget 安装，用于尝试修复） |
| Python | 3.13.5 (Anaconda) |
| 系统区域 | zh-CN |
| 系统 UTF-8 设置 | 已开启（ACP/OEMCP 均为 65001） |

### 用户工作流程

1. 在 Cursor 底部打开终端标签页（Terminal）
2. 输入 `claude --dangerously-skip-permissions` 启动 Claude Code
3. Claude Code 在终端 TUI 中运行，用户在终端里跟它对话
4. 需要粘贴中文时 → 乱码

---

## 三、根因分析

### 官方 Bug

这是 **Cursor 的已知 Bug**，2025年11月首次上报，团队已确认但截至2026年6月仍未修复。

- 论坛帖子：https://forum.cursor.com/t/windows-cursor-hooks-corrupt-chinese-utf-8-characters-in-stdin-replaced-with-question-marks/142878
- 影响所有非 ASCII 语言（中文、日文、韩文、西里尔文等）
- 根因：Cursor 在 Windows 上通过 ConPTY 传递粘贴内容时，编码转换层将 UTF-8 多字节序列错误地按单字节处理

### 触发时机

- **Cursor 3.8.22 更新后开始出现**（6月22日更新）
- 之前用一个多月完全正常
- 也可能与 Claude Code 扩展 2.1.185/2.1.186 更新有关（6月21日更新）

---

## 四、关键发现：英文粘贴热身法

**这是目前唯一可复现的临时解决方案。**

### 现象

每个新的终端会话中：
1. **第一次粘贴中文** → 乱码 
2. **先粘贴一段纯英文** → 英文正常显示
3. **之后粘贴中文** → 正常 ✅

### 原理分析

xterm.js 的粘贴处理管线存在**懒初始化**问题：

```
终端启动 → 粘贴管线处于"未初始化"状态
         → UTF-8 多字节处理逻辑未激活
         → 第一个粘贴的字节被当作单字节处理（Latin-1）

第一次粘贴英文 → 全是 ASCII 单字节（0x00-0x7F）
               → 单字节处理恰好是正确的
               → 触发了粘贴管线的初始化/模式切换
               → UTF-8 多字节处理被激活

第二次粘贴中文 → 管线已初始化，UTF-8 处理正常 ✅
```

### 注意事项

- 必须是**真正的剪贴板粘贴**（Ctrl+V），不是键盘输入
- 在终端里敲 `a` 回车不能触发，必须是粘贴操作
- 在 `.bashrc` 里用 `printf '\e[200~ok\e[201~'` 模拟粘贴序列也无效
- 用 `clip.exe` 自动放英文到剪贴板后让用户按一次 Ctrl+V 也无效（用户反复测试确认）

**结论：只有用户手动 Ctrl+V 粘贴英文才能触发初始化，自动化方案全部失败。**

---

## 五、已尝试的所有方案（全部失败）

| # | 方案 | 操作 | 结果 |
|---|------|------|------|
| 1 | 终端字体配置 | `settings.json` 加 `terminal.integrated.fontFamily` 含中文字体 | ❌ 无效 |
| 2 | 禁用 bracketed paste | `terminal.integrated.enableBracketedPaste: false` | ❌ 无效 |
| 3 | PowerShell UTF-8 启动参数 | `args: ["-NoExit", "-Command", "chcp 65001 > $null"]` |  无效 |
| 4 | Reload Window | `Ctrl+Shift+P` → `Developer: Reload Window` | ⚠️ 偶尔有效但不稳定 |
| 5 | Windows 系统 UTF-8 | 区域设置 → Beta: 使用 Unicode UTF-8 提供全球语言支持 → 重启 |  无效（ACP/OEMCP 已变为 65001） |
| 6 | MSYS=disable_pcon | `terminal.integrated.env.windows` 设 `MSYS: "disable_pcon"` | ❌ 无效 |
| 7 | Panel ↔ Terminal 切换 | `claudeCode.preferredLocation` 在 panel/terminal 间切换 | ❌ 无效 |
| 8 | PowerShell 7 | winget 安装 PS7，原生 UTF-8 | ❌ 无效 |
| 9 | CMD 终端 | 改默认终端为 CMD + `chcp 65001` | ❌ 更差（次次乱码） |
| 10 | 回退 Cursor 版本 | 安装 3.7.42（3.8.22 之前） | ❌ 仍然乱码 |
| 11 | PYTHONUTF8=1 | `terminal.integrated.env.windows` 设环境变量 | ❌ 无效 |
| 12 | 模拟粘贴序列 | `.bashrc` 加 `printf '\e[200~ok\e[201~'` | ❌ 无效（未触发初始化） |
| 13 | clip.exe 自动放英文 | `.bashrc` 加 `echo -n "ok" \| clip.exe` | ❌ 无效（用户反复测试） |

---

## 六、当前状态

### settings.json（已还原为原始状态）

```json
{
    "window.autoDetectColorScheme": true,
    "security.promptForLocalFileProtocolHandling": false,
    "editor.largeFileOptimizations": false,
    "workbench.preferredHighContrastColorTheme": "Default Dark Modern",
    "workbench.preferredDarkColorTheme": "Monokai Dimmed",
    "workbench.preferredLightColorTheme": "Monokai Dimmed",
    "workbench.editorAssociations": {
        "*.docx": "default",
        "*.pdf": "default"
    },
    "claudeCode.preferredLocation": "panel",
    "markdown-pdf.breaks": true,
    "markdown-preview-enhanced.previewTheme": "atom-light.css",
    "markdown-preview-enhanced.customCss": "markdown-custom.css",
    "markdown-preview-enhanced.revealjsTheme": "vscode.css",
    "markdown-preview-enhanced.enablePreviewZenMode": true,
    "markdown-preview-enhanced.codeBlockTheme": "github-dark.css",
    "markdown-preview-enhanced.enableScriptExecution": true,
    "markdown-preview-enhanced.chromePath": "C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe"
}
```

位置：`C:\Users\www15\AppData\Roaming\Cursor\User\settings.json`

### .bashrc（包含失败的 workaround）

```bash
PS1='\[\033[1;32m\]\u@\h\[\033[0m\]:\[\033[1;34m\]\w\[\033[0m\]\[\033[1;31m\]$(__git_ps1 " (%s)")\[\033[0m\]\n\$ '

# Cursor 终端粘贴中文 Bug workaround：自动将英文放入剪贴板
echo -n "ok" | clip.exe 2>/dev/null
```

位置：`C:\Users\www15\.bashrc`

### .claude/settings.json（Claude Code 配置）

```json
{
  "effortLevel": "low",
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "sk-8f14eed3c33d470ea0bc1548001fab22",
    "ANTHROPIC_BASE_URL": "https://dashscope.aliyuncs.com/apps/anthropic",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "qwen3.6-flash",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL_NAME": "qwen3.6-flash",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "qwen3.7-plus[1M]",
    "ANTHROPIC_DEFAULT_OPUS_MODEL_NAME": "qwen3.7-plus",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "qwen3.7-plus[1M]",
    "ANTHROPIC_DEFAULT_SONNET_MODEL_NAME": "qwen3.7-plus",
    "ANTHROPIC_MODEL": "qwen3.7-plus[1M]"
  },
  "includeCoAuthoredBy": false,
  "model": "qwen3.6-plus[1M]",
  "skipDangerousModePermissionPrompt": true,
  "theme": "light"
}
```

位置：`C:\Users\www15\.claude\settings.json`

---

## 七、未尝试的方向（供后续排查）

1. **直接用 Windows Terminal 跑 Claude Code** — 不走 Cursor 终端，完全绕过 Bug
2. **Cursor 更新日志** — 查看 3.8.22 的 release notes，确认是否有终端相关改动
3. **xterm.js 版本** — 检查 Cursor 使用的 xterm.js 版本，看是否有已知修复
4. **降级 Claude Code 扩展** — 回退到 2.1.184 或更早版本
5. **VS Code + Claude Code 扩展** — 用 VS Code 代替 Cursor，测试是否同样有问题（如果 VS Code 正常，则确认是 Cursor 特有的 Bug）
6. **终端编码调试** — 用 `script` 或 `strace` 级别的工具追踪粘贴时的字节流
7. **ConPTY 调试** — 检查 Windows ConPTY 的编码设置

---

## 八、给下一个 AI 的提示

用户的核心需求：**在 Cursor 终端里粘贴中文不乱码**。

已知事实：
- 这是 Cursor 的已知 Bug，官方未修复
- 先粘贴英文再粘贴中文可以临时解决（手动操作）
- 所有自动化方案都失败了
- 用户已经尝试了几乎所有常见解决方案

建议优先尝试：
1. 用 Windows Terminal 直接运行 `claude`，完全绕过 Cursor 终端
2. 检查是否是 Claude Code 扩展更新导致的（降级扩展版本）
3. 检查 VS Code 是否有同样问题（区分是 Cursor 还是 VS Code 上游的问题）
